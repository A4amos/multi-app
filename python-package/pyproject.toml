[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "smartmath-python"
version = "1.0.0"
description = "A small Python mathematics package"
requires-python = ">=3.9"

[tool.setuptools]
packages = ["smartmath"]